apiVersion: apps/v1
kind: Deployment
metadata:
  name: jackpothub-cloud-ppc-social
  labels:
    app: jackpothub-cloud-ppc-social
spec:
  replicas: 1
  selector:
    matchLabels:
      app: jackpothub-cloud-ppc-social
  template:
    metadata:
      labels:
        app: jackpothub-cloud-ppc-social
    spec:
      containers:
        - name: jackpothub-cloud-ppc-social
          image: IMAGE_PLACEHOLDER
          ports:
            - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: jackpothub-cloud-ppc-social
  labels:
    app: jackpothub-cloud-ppc-social
spec:
  type: LoadBalancer
  selector:
    app: jackpothub-cloud-ppc-social
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
  loadBalancerIP: LOAD_BALANCER_IP_PLACEHOLDER
